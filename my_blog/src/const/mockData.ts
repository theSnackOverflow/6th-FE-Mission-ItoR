// mockData deprecated — keep empty export to avoid breaking imports during transition.
// Remove any imports of `mockData` and rely on `getAllPosts` API instead.
export const mockData: never[] = [];
