import LoginModal from '../../components/Modal/LoginModal';

const TestForLoginModal = () => {
  return (
    <main className="flex flex-col items-center justify-center h-screen gap-4 bg-[#E2DCFF]">
      <LoginModal />
    </main>
  );
};

export { TestForLoginModal };
