import Menu from '../../components/Menu';

const TestForMenu = () => {
  return (
    <main className="flex flex-col items-center justify-center h-screen gap-4 bg-gray-500">
      <Menu />
    </main>
  );
};
export default TestForMenu;
